# Generated by the harness. All of it is reproducible from the pinned configs,
# the split manifest and the seed; none of it belongs in git.

# scripts/01_prepare_data.py output (rendered JSONL + split_manifest.json)
data/

# run directories: adapters, checkpoints, merged weights, generations, metrics,
# tensorboard logs, runs/env_check.json, runs/comparisons/
runs/

# local caches
.cache/
__pycache__/
.pytest_cache/
